# To make all dependencies for the Django project install automagically,
# we might add some more files, with exact commands (also to generate data, sync the database, perform migrations...)

# Let's install the project dependecies from pip. Globally. Or use virtualenv.
#sudo /usr/bin/pip install -r $PROJECT_DIR/requirements/local.txt
#  cmd.run

# ...
