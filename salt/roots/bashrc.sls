# set environment variables here
#echo "export SOME_VAR=value" >> /home/vagrant/.bashrc:
#  cmd.run

# activate the virtual env for convenience
echo "source /home/vagrant/venv/bin/activate" >> /home/vagrant/.bashrc:
  cmd.run
